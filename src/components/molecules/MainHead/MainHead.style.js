import styled from 'styled-components';

const MainHeadStyle = styled.div`
  border-bottom: solid 1px ${props => props.theme.COLOR.LIGHT_GRAY};
  font-size: 1rem;
  font-weight: bold;
  margin: -15px -15px 15px -15px;
  padding: 15px;
`;

export default MainHeadStyle;
